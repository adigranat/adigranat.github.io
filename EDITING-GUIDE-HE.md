# מדריך עריכה — Signature V6

## שינוי טקסט
פתחו את `index.html`, חפשו את המשפט הרצוי באמצעות `Ctrl + F`, החליפו רק את הטקסט ושמרו עם `Ctrl + S`.

## שינוי צבעים
בתחילת `style.css` נמצאים המשתנים:

```css
--ink: #223735;
--sage: #456861;
--cream: #f8f4ee;
--gold: #b58a62;
```

## שינוי תמונה
שמרו תמונה בשם `adi-granat.jpg` בתיקיית `assets`, ואז החליפו ב-`index.html` את כתובת ה-Wix ב:

```html
src="assets/adi-granat.jpg"
```

## בדיקה מהירה
פתחו את התיקייה ב-VS Code והפעילו `Go Live`. לאחר כל שינוי לחצו `Ctrl + S`.

## GitHub Pages
העלו את כל הקבצים לשורש המאגר, וב-Settings → Pages בחרו `main` ו-`/ (root)`.
